[project]
name = "maw-test-fixture"
version = "0.1.0"
description = "Intentionally buggy Python project for testing MAW Native"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
